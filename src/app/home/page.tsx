'use client'

import React, { useEffect, type ReactElement } from 'react'

export default function Home (): ReactElement {
  useEffect(() => {
    // loadPosts()
  }, [])

  return (
    <div>

    </div>
  )
}
